export class Signature {
    UID;
    userUID;
    initiativeUID;
    signature;
    constructor(UID, userUID, initiativeUID, signature) {
        this.UID = UID;
        this.userUID = userUID;
        this.initiativeUID = initiativeUID;
        this.signature = signature;
    }
}
