export class User {
    UID;
    firstName;
    lastName;
    constructor (UID, firstName, lastName) {
        this.UID = UID;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}