export class Tag {
    UID;
    initiativeUID;
    text;
    constructor(UID, initiativeUID, text) {
        this.UID = UID;
        this.initiativeUID = initiativeUID;
        this.text = text;
    }
}